<div class="container">
    <h1>Clients#index</h1>
    <p>Find me in app/views/clients/index.html.erb</p>
    <h1>Para el Cervecero</h1>

    <% @clients.each do |c| %>
        <%= c.name %>
        <%= link_to "#{c.deals.count}", show_deals_path(c.id) %>
    <% end %> 

</div>

