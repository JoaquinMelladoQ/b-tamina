<h2>Check your deals with <%= @client.name %></h2>

<% @deals.each do |d| %>
<div class="card">
    <div class="card-body">
      <h5 class="card-title">Quantity: <%= d.deal_quantity %></h5>
      <p class="card-titlet">Deal date: <%= d.deal_date %>.</p>
    </div>
  </div>

<% end %>

